name: CI/CD Deployment Pipeline

on:
  workflow_dispatch:  # 手動觸發（建議這樣做，方便測試）

jobs:
  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest
    outputs:
      VERSION: ${{ steps.version.outputs.VERSION }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 安裝 Bun
        run: curl -fsSL https://bun.sh/install | bash

      - name: 設定 Bun 路徑
        run: echo "$HOME/.bun/bin" >> $GITHUB_PATH

      - name: Get version from package.json or git
        id: version
        run: |
          # 如果 package.json 存在且有 version 欄位就取用
          if [ -f "package.json" ] && node -e "v=require('./package.json').version; if(!v) process.exit(1)" 2>/dev/null; then
            VERSION=$(node -p "require('./package.json').version")
          else
            # 否則從 git tag 取，若沒有 tag 就用 commit hash
            GIT_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
            if [ -n "$GIT_TAG" ]; then
              VERSION="${GIT_TAG#v}"   # 去除前面的 v
            else
              VERSION="0.0.0-$(git rev-parse --short HEAD)"
            fi
          fi
          echo "✅ Version detected: $VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "version=$VERSION" >> $GITHUB_OUTPUT
      - name: Install dependencies & build
        run: |
          npm install
          npm run build
          zip -r build-${{ env.VERSION }}.zip .
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-artifact
          path: build-${{ env.VERSION }}.zip

  deploy-staging:
    name: 🧪 Deploy to Staging
    needs: build
    runs-on: ubuntu-latest
    environment: staging
    env:
      VERSION: ${{ needs.build.outputs.VERSION }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: 安裝 Bun
        run: curl -fsSL https://bun.sh/install | bash

      - name: 設定 Bun 路徑
        run: echo "$HOME/.bun/bin" >> $GITHUB_PATH

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build-artifact

      - name: Install GitHub CLI
        run: sudo apt install gh -y

      - name: Create Staging Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="staging-${VERSION}"
          NOTE="🧪 Staging build for version ${VERSION}"
          echo "Creating release ${TAG}"
          gh release create "$TAG" "build-${VERSION}.zip" --notes "$NOTE"
  deploy-production:
    name: 🚀 Deploy to Production
    needs: [build, deploy-staging]  # ✅ 同時依賴 build（才能取到輸出）
    runs-on: ubuntu-latest
    environment: production   # ✅ 有審核機制
    env:
      VERSION: ${{ needs.build.outputs.VERSION }}   # ✅ 從 build 拿
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: 安裝 Bun
      run: curl -fsSL https://bun.sh/install | bash

    - name: 設定 Bun 路徑
      run: echo "$HOME/.bun/bin" >> $GITHUB_PATH

    - name: Download build artifact
      uses: actions/download-artifact@v4
      with:
        name: build-artifact

    - name: Install GitHub CLI
      run: sudo apt install gh -y

    - name: Create production release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        echo "🧩 Using version: ${VERSION}"
        TAG="v${VERSION}"
        NOTE="🚀 Production release for version ${VERSION}"
        echo "Creating release ${TAG}"

        if [ ! -f "build-${VERSION}.zip" ]; then
          echo "❌ Build file not found: build-${VERSION}.zip"
          exit 1
        fi

        gh release create "$TAG" "build-${VERSION}.zip" --notes "$NOTE" || {
          echo "⚠️ Release failed, rollbacking..."
          gh release delete "$TAG" -y || echo "⚠️ Rollback skipped (release not found)"
          exit 1
        }
